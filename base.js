class Base {
    /**
     * 
     * @param {String} nombre 
     * @param {Number} minutos 
     */

    constructor(nombre, minutos) {
        this.nombre = nombre;
        this.minutos = minutos
    }
}